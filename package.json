{
  "name": "usfansindex-com",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "node scripts/check-data.mjs && astro build && node scripts/check-routes.mjs",
    "preview": "astro preview --host 127.0.0.1",
    "preview:local": "node scripts/local-preview.mjs",
    "check:data": "node scripts/check-data.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
